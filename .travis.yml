sudo: required
services:
- docker
script:
- docker build -t kipchichirian/node .
- docker images kipchirchirian/node
before_deploy:
  - echo "$DOCKER_PASS" | docker login --username kipchirchirian --password-stdin
deploy:
  provider: script
  script: docker push kipchirchirian/node
  on:
    branch: main